const sectionPrefix = 'add-todo'

export const INPUT_SECTION_IDS = {
    ATD_INPUT_FIELD : `${sectionPrefix}-input-field`,
    ATD_BUTTON : `${sectionPrefix}-button`,
    ATD_ERROR_MESSAGE : `${sectionPrefix}-error-message`
}

export const INPUT_SECTION_VALIDATION_MESSAGES = {
    EMPTY_FIELD_SUBMIT : "Cannot add empty value"
}